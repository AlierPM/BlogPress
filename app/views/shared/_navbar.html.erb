<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <div class= "flex flex-row items-center">
      <%= link_to "BlogPress", root_path, class: "navbar-brand" %>
      <%= link_to "Authors", users_path, class: "nav-link" if user_signed_in? %>
    </div>

    <div>
      <% if user_signed_in? %>
        <%= link_to "Create a new post", new_user_post_path(current_user), class: "btn btn-primary" %>
        <%# <%= link_to "See all posts", user_posts_path(current_user), class: "btn btn-primary" %>
        <%= link_to "Sign Out", destroy_user_session_path, data: {turbo_method: :delete}, class: "btn btn-primary" %>
      <% else %>
        <%= link_to "Sign up", new_user_registration_path, class: "btn btn-primary" %>
        <%= link_to "Sign in", new_user_session_path, class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>
</nav>
